---
- name: Create a keypair
  host: local
  ec2_key:
    name: newkey1

- name: Save private key
  copy: content="{{ ec2_key_result.key.private_key }}" dest="~/.ssh/newket1.pem" mode=400
  when: ec2_key_result.changed
