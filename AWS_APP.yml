---

- name: install builder
  hosts: local
  become: yes
  gather_facts: False

  tasks:
  - name: Create a keypair
    ec2_key:
      name: newkey1

  - name: Save private key
    copy:
      content: "{{ ec2_key.key.private_key }}"
      dest: "~/.ssh/newket1.pem"
      mode: 0600

